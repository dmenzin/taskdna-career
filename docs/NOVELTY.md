# Novelty

Discovery novelty rewards plausible non-obvious matches, not randomness. Inputs include title/domain distance, Task-DNA similarity, capability transfer plausibility, and whether the job is too obvious from the user's history.

The UI includes a "Non-obvious only" filter and novelty explanations for selected jobs.
