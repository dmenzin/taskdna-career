# Commute

V1 includes commute semantics in the UI and scoring model boundary, with synthetic remote/hybrid/onsite work modes. Exact home addresses are not stored in committed fixtures.

Next refinement: implement `SimulatedCommuteProvider` as a dedicated provider with configurable time bands and commute penalties.
