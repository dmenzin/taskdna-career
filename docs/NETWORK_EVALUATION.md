# Network evaluation

Run:

```bash
pnpm eval:network
```

The harness checks:

- former manager/direct ask behavior,
- explicit offer -> send requested materials,
- referral boundaries respected,
- network access does not alter Work Fit,
- second-degree paths exist,
- next-best-action portfolio is balanced.

Synthetic evaluations are regression checks, not proof of real-world networking effectiveness.
