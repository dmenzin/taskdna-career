# Opportunity Graph

V2 adds a Human Opportunity Graph that connects the existing self/job intelligence to synthetic contacts, relationships, organizations, teams, interactions, direct paths, second-degree paths, pursuit plans, and next-best actions.

Important invariant: network access never changes intrinsic Work Fit. Access affects pursuit strategy and action priority only.

The sandbox graph is deterministic and uses fictional contacts only.
