# Debugging

The app includes a debug/product lab section with:

- selected recommendation decision trace,
- profile confidence,
- search funnel counts,
- freshness semantics,
- product hypotheses and limitations.

For a recommendation, the trace explains evidence profile -> job function -> Task-DNA similarity -> hireability inputs -> negative-fit risk -> action tier.
