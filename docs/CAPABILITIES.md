# Capabilities

Capabilities are independent from preferences.

The UI and engine distinguish:

- work the user has done,
- work the user has done successfully,
- work the user enjoyed,
- work the user might enjoy but has not demonstrated.

Active-learning feedback can update Task-DNA preferences but does not create new capability evidence. This is covered by regression tests.
